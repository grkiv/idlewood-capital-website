# Prevent search engine indexing of investor updates
Header always set X-Robots-Tag "noindex, nofollow, nosnippet, noarchive"

# Optional: Add basic access control
# Uncomment and configure if you want additional security
# AuthType Basic
# AuthName "Investor Updates"
# AuthUserFile /path/to/.htpasswd
# Require valid-user